import _JSON$stringify from 'babel-runtime/core-js/json/stringify';
var index = require('./');

index.save = function (db, destination) {
  destination = destination || 'db';
  localStorage.setItem(destination, _JSON$stringify(db, null, 2));
};

index.load = function (source) {
  source = source || 'db';
  return JSON.parse(localStorage.getItem(source));
};

_.mixin(index);

//# sourceMappingURL=browser-compiled.js.map